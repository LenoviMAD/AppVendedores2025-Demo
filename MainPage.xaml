<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:components="clr-namespace:AppVendedores2025.Components"
    Style="{StaticResource SafeAreaPageStyle}"
    x:Class="AppVendedores2025.MainPage"
    BackgroundColor="#111827">

    <!-- Un solo contenido: un Grid con el WebView y el recuadro flotante -->
    <Grid>
        <!-- Contenido principal -->
        <BlazorWebView x:Name="Bwv"
                       HostPage="wwwroot/index.html"
                       BackgroundColor="Transparent"
                       HorizontalOptions="FillAndExpand"
               VerticalOptions="FillAndExpand">
            <BlazorWebView.RootComponents>
                <RootComponent Selector="#app" ComponentType="{x:Type components:Routes}" />
            </BlazorWebView.RootComponents>
        </BlazorWebView>            
    </Grid>
</ContentPage>
